name: bntu_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core:
  cloud_firestore:
  firebase_auth:
  firebase_storage:
  custom_splash: ^0.0.2
  yandex_mapkit: ^1.1.1
  provider: ^6.0.0
  fluttertoast: ^8.0.8
  image_picker:
  url_launcher: ^6.0.10
  shared_preferences: ^2.0.8
  carousel_slider: ^4.0.0
  mockito: ^5.0.16 # testing
  flutter_staggered_animations: "^1.0.0"
  auto_size_text: ^3.0.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_launcher_icons: ^0.9.2
  flutter_test:
    sdk: flutter
  build_runner:
  pedantic:

  get: ^4.1.4

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/BNTU_Logo.png"

flutter:
  uses-material-design: true

  assets:
     - assets/
     - assets/yandex_images/place.png

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
